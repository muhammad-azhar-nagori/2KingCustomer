import 'package:flutter/material.dart';

class ContractorDetails extends StatelessWidget {
  const ContractorDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
